/**
 * The workbench Extension.
 *
 * Built in, in the sense spec/extension.md §7 means: shipped with mesh-web, and **privileged only in
 * being present by default** — it is written against the same interfaces an outside author gets,
 * which is the only honest test that those interfaces are usable.
 */

export * from './extension.js';
