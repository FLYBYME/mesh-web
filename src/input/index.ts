export type { Binding, BindingTable, Chord, GamepadBinding } from './keys.js';
export {
    BROWSER_TAB_RESERVED, InvalidBinding, NOTHING_RESERVED,
    bindingTable, chordOf, formatBinding, isGamepad, normalizeBinding, parseBinding, reservedSet,
} from './keys.js';
